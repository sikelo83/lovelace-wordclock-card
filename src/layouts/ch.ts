import type { Layout } from '../types';

// Phase 2: Bärndütsch grid + time logic based on
// https://github.com/manuelmeister/WordClockBerndeutsch (src/layout.cpp)
// Note: hour switches to the next one from :25 ("füf vor haubi sächsi")
export const ch: Layout = {
  grid: [],
  timeToWords: () => [],
};
